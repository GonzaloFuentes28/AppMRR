# AppMRR Environment Variables Example
# Copy this file to .env and fill in your values

# =====================================================
# REQUIRED: Encryption Key
# =====================================================
# Used to encrypt RevenueCat API keys before storing them
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ENCRYPTION_KEY=your-64-character-hex-string-here

# =====================================================
# DATABASE CONFIGURATION (choose one)
# =====================================================

# Option 1: Supabase (Recommended for Production)
# Get these from your Supabase project settings -> API
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here
# Note: Service role key bypasses RLS and should be kept secret

# Option 2: PostgreSQL Connection String
# For local development or custom PostgreSQL setup
# POSTGRES_URL=postgresql://postgres:password@localhost:5432/appmrr

# Option 3: Individual PostgreSQL Connection Parameters
# Alternative to POSTGRES_URL
# POSTGRES_HOST=localhost
# POSTGRES_PORT=5432
# POSTGRES_DATABASE=appmrr
# POSTGRES_USER=postgres
# POSTGRES_PASSWORD=your-password

# =====================================================
# OPTIONAL: Cron Job Security
# =====================================================
# Secret token to protect the cron endpoint
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# Vercel Cron Jobs automatically include this in Authorization header
CRON_SECRET=your-random-secret-here

# =====================================================
# DEVELOPMENT NOTES
# =====================================================
# 
# Local Development:
# - Use PostgreSQL with POSTGRES_URL
# - Run ./setup-local.sh to create database and schema
# - Start dev server: npm run dev
#
# Production (Vercel + Supabase):
# - Use SUPABASE_URL and SUPABASE_SERVICE_ROLE_KEY
# - Set all variables in Vercel dashboard
# - Deploy: git push origin main
#
# Security:
# - Never commit this file with real values
# - Keep ENCRYPTION_KEY and SUPABASE_SERVICE_ROLE_KEY secret
# - Rotate keys if compromised
